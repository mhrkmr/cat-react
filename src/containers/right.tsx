import Headline from "../components/product-catalog/headline";

const Right = () => {
	return (
		<div className="right">
			<Headline text="beta gamma delta epsilon" />
		</div>
	);
};

export default Right;
